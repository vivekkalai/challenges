pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                 
                 sh '''docker info 

                       docker build .

                       docker images'''

            }

        }
    }
}
