pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                 
                 sh '''docker info 

                       docker build -t jenkins-demo:${BUILD_NUMBER} . 

                       docker images'''

            }

        }
    }
}
